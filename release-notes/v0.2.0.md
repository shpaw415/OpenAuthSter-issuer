## imporovement

- `add user endpoint` to get specific user with the secret key.
- remake all endpoints using hono. ( much cleaner )

## Breaking changes

- need to update `openauthster-shared` to version 0.2.x
- need to update `openauth-webui` to version 0.2.x

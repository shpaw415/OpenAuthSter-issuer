# Release Notes - v0.3.0

**Release Date:** February 20, 2026

## 🎉 New Features

### Webhook Support

Added webhook functionality to enable real-time event notifications:

- **Webhook Registration & Management** - Configure webhooks for your client application
  - Register webhook endpoints to receive event notifications
  - Support for multiple webhook listeners per client
  - Secure webhook delivery with authentication
- **Event-Driven Architecture** - Subscribe to system events as they occur
  - User authentication events
  - Session updates
  - User management operations
  - Configurable event filtering and routing
- **Secure Webhooks** — Full event orchestration with HMAC SHA-256 signatures
- **Replay Protection** — Integrated timestamp verification (5-minute window) to prevent malicious request re-use

### Version Endpoint Enhancement

Added enhanced `/version` endpoint for WebUI version monitoring:

- **GET** `/version` - Returns current OpenAuthster issuer version
  - Enables WebUI to continuously monitor for new versions on the server
  - Supports automated version checking and update notifications
  - Includes comprehensive CORS policy for cross-origin requests
  - Returns version information in JSON format for easy client-side parsing

## 🔧 Major Improvements

### CORS Policy Implementation

- Implemented comprehensive CORS (Cross-Origin Resource Sharing) policy
- Enhanced security for cross-origin requests from WebUI applications
- Enables seamless communication between frontend and backend services

## 🧪 Test Suite

Added a comprehensive test suite for the CLI commands using Bun's test runner.

### CLI Init Flow (`tests/init-flow.test.ts`)

Unit tests for the `initialize` command covering:

- **Wrangler method happy path** — exact exec command sequence, `wrangler.json` content, success logs, no git contamination
- **Git method happy path** — full command sequence including git setup, D1 provisioning, and initial push
- **Binary checks** — missing `wrangler` or `git` binaries produce the correct error and `exit(1)`
- **Repository URL validation** — missing `--repo` on the git method exits early with an informative error
- **Exec error handling** — each individual command failure produces the correct error message and stops execution
- **Jurisdiction / location forwarding** — parameterized tests confirming all option combinations are passed to `wrangler d1 create`
- **Integration tests** — clones the real repository into a temp directory and runs `initializeFlow` with real filesystem I/O (mocked external commands) to verify `wrangler.json` is written correctly from the actual `wrangler.example.jsonc`

### CLI Upgrade Flow (`tests/upgrade-flow.test.ts`)

Unit tests for the `upgrade` command covering:

- **deploy: none** — correct exec sequence, manual deploy hint logged, no deploy commands run
- **deploy: wrangler** — `wrangler deploy` is invoked after pull + migration, git push is not
- **deploy: git** — `git push` is invoked after pull + migration, wrangler deploy is not
- **Version forwarding** — parameterized tests ensuring the version string reaches `git pull` correctly
- **Binary checks** — missing `git` binary produces the correct error and `exit(1)` without running any commands
- **Exec error handling** — failures on `git pull`, `wrangler d1 apply`, `wrangler deploy`, and `git push` each exit with code 1, emit the correct error message, and halt further execution

### Testable Architecture

The CLI logic for both `initialize` and `upgrade` commands has been extracted into standalone, dependency-injected functions (`bin/initFlow.ts`, `bin/upgradeFlow.ts`), enabling full unit testing without spawning real processes or requiring Cloudflare credentials.

### Webhook Security Layer (`tests/webhooks/index.test.ts`)

- **Reliability** — Added comprehensive test suite for the webhook security layer covering signature verification, replay protection, and payload parsing

### security update

> I am working on the security features that will be ISO 27001 complient

- removed secret in clear text in header x-secret
- webhook body signed with HMAC sha-256 with the app-secret
- endpoint needing secret has signature check with timestamp ( relay attack resistant )
- Step-up Authentication with Proof-of-MFA

### MFA ( Multi factor Auth )

> MFA is setup by the user and encrypted backup code are stored for removing/modifying the MFA

#### Step Up MFA

Endpoints can be protected with MFA.
User get an elevated token created from a TOTP with a 5 minutes window to call the protected endpoint.

#### Remove MFA

There is 2 ways to remove MFA:

> in both senario the user must be logged with a valid token

- With an elevated token
- With a backup code
